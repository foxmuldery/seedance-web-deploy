# 火山 TOS 图生视频外链开通指引

更新时间：2026-05-22  
用途：为 Seedance 图生视频提供公网可访问的临时图片外链。  
使用方式：本地测试网页先把本地图片上传到火山 TOS，再生成临时签名 URL，然后把该 URL 传给火山方舟 Seedance API。

## 1. 需要开通什么

请在火山引擎后台开通：

1. 对象存储 TOS 服务。
2. 一个北京区域的 TOS Bucket。
3. 一个只用于该 Bucket 的最小权限 AccessKey/SecretKey。
4. 可选：生命周期规则，自动删除测试图片。

## 2. 推荐配置

### Bucket

建议创建：

```text
区域：华北2（北京）/ cn-beijing
读写权限：私有
Bucket 名：seedance-inputs-公司名或项目名
```

说明：

- Bucket 不需要公共读。
- 本地工具会生成临时签名 URL，Seedance 在有效期内可以读取图片。
- 不建议使用公共读 Bucket，避免图片外链长期暴露。

### Endpoint

北京区域建议使用：

```text
https://tos-cn-beijing.volces.com
```

### 对象前缀

建议统一放到：

```text
seedance-inputs/
```

工具上传后的对象路径类似：

```text
seedance-inputs/20260522/随机ID_文件hash_原文件名.png
```

### 签名 URL 有效期

建议：

```text
86400 秒
```

也就是 1 天。只用于测试和生成，不建议长期有效。

## 3. 权限要求

请创建一个专用 AK/SK，不要直接给主账号全权限 AK。

该 AK/SK 至少需要对目标 Bucket 有以下权限：

```text
tos:PutObject
tos:GetObject
tos:ListBucket
```

说明：

- `PutObject`：上传本地图片到 TOS。
- `GetObject`：生成签名 URL 后，Seedance 能读取图片。
- `ListBucket`：本地网页里的“检测 TOS 配置”会用它测试 Bucket 是否可访问。

如果不想开放 `ListBucket`，也可以不使用“检测 TOS 配置”，但上传功能仍至少需要 `PutObject` 和 `GetObject`。

建议限制资源范围到指定 Bucket 和指定前缀：

```text
Bucket: seedance-inputs-公司名或项目名
Prefix: seedance-inputs/*
```

## 4. 生命周期规则

建议给 `seedance-inputs/` 前缀加生命周期：

```text
自动删除：1-3 天后删除
```

原因：

- 这些图片只是 Seedance 图生视频的临时输入素材。
- 避免长期累计存储费用。
- 降低图片外链泄露风险。

## 5. 提供给测试同事的信息

请开通后提供以下信息：

```text
TOS Bucket:
TOS Region: cn-beijing
TOS Endpoint: https://tos-cn-beijing.volces.com
TOS AccessKey:
TOS SecretKey:
Security Token: 如果是临时凭证则提供；长期 AK/SK 可留空
对象前缀: seedance-inputs
签名 URL 有效期: 86400
```

注意：

- AccessKey 和 SecretKey 属于敏感信息，请通过公司内部安全渠道传递。
- 不要发到群聊、文档评论区或公开 IM。
- 如果可以，优先提供临时凭证或最小权限子账号。

## 6. 本地测试网页填写方式

打开本地网页：

```text
http://127.0.0.1:8767
```

在 TOS 配置区填写：

```text
TOS Bucket = 提供的 bucket 名
TOS Region = cn-beijing
TOS Endpoint = https://tos-cn-beijing.volces.com
TOS AccessKey = 提供的 AK
TOS SecretKey = 提供的 SK
对象前缀 = seedance-inputs
外链有效期 = 86400
```

然后点击：

```text
检测 TOS 配置
```

通过后，图生视频流程为：

```text
选择图生视频
选择本地图片
填写提示词
创建任务
```

本地网页会自动完成：

```text
本地图片 -> 上传 TOS -> 生成临时签名 URL -> 提交给 Seedance API
```

## 7. 成本说明

TOS 会产生费用，但该用途成本通常很低。

主要计费项：

```text
存储容量
请求次数
外网流出流量
```

对于单张 2-5MB 图片，上传一次、Seedance 读取一次、存放 1 天，成本通常远低于视频生成费用。主要成本仍是 Seedance 视频生成。

为控制成本：

1. Bucket 保持私有。
2. 使用临时签名 URL。
3. 给 `seedance-inputs/` 加 1-3 天自动删除。
4. 不开启 CDN、加速器、图片处理等额外服务。

## 8. 验收标准

开通完成后，本地测试应满足：

1. “检测 TOS 配置”返回成功。
2. 选择本地图片后创建图生视频任务，不再传 base64，而是传 TOS 签名 URL。
3. Seedance 任务可以创建成功。
4. 生成任务返回 `task_id`。
5. 查询任务成功后能下载结果视频。

