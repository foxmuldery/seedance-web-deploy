# 兔狲视频生成器网站部署说明

这是一个带后端的工具，不适合当作纯静态网页部署。推荐部署在火山 ECS、阿里云 ECS、腾讯云 CVM、海外 VPS 或公司内网服务器上，用 Docker 运行。

## 1. 需要上传的文件

只需要上传这些文件：

- `volcengine_seedance_web.py`
- `requirements-seedance-web.txt`
- `Dockerfile.seedance-web`
- `docker-compose.seedance-web.yml`
- `.env.seedance-web.example`
- `deploy_seedance_web_to_server.sh`

不要上传本机的 `.seedance_web_config.json`、`.env.seedance-web`、输出视频目录和日志目录。

## 2. 服务器准备

服务器需要安装 Docker 和 Docker Compose。建议部署在中国大陆或离火山北京区域更近的服务器，TOS/方舟链路会稳定很多。

## 3. 配置密钥

在服务器目录里复制环境变量模板：

```bash
cp .env.seedance-web.example .env.seedance-web
```

然后编辑 `.env.seedance-web`，填入：

```bash
ARK_API_KEY=你的火山方舟 API Key
TOS_ACCESS_KEY_ID=你的 TOS AccessKey
TOS_SECRET_ACCESS_KEY=你的 TOS SecretKey
TOS_BUCKET=你的 bucket
```

这些密钥只放服务器环境变量，不要写进前端页面、不要提交到仓库。

## 4. 启动

```bash
docker compose -f docker-compose.seedance-web.yml up -d --build
```

默认服务端口是 `8080`。访问：

```text
http://服务器IP:8080/
```

如果要绑定域名，建议用 Nginx 或 Caddy 做反向代理到 `127.0.0.1:8080`，并开启 HTTPS。

如果本机已经能 SSH 登录服务器，也可以直接在本机项目目录运行：

```bash
./deploy_seedance_web_to_server.sh 207.148.106.142 root /opt/seedance-web
```

如果本机没有 `.env.seedance-web`，脚本会先上传代码，然后提示你到服务器里编辑 `.env.seedance-web` 再启动。

## 5. 日志与输出

容器里的持久数据会保存在 Docker volume `seedance-web-data`。

默认路径：

- 输出目录：`/data/seedance_web_outputs`
- 全局日志：`/data/seedance_web_logs/seedance_web_events.jsonl`
- 每个任务日志：`/data/seedance_web_outputs/<run_id>/events.jsonl`

页面上也会显示当前任务日志路径。

## 6. 权限建议

如果多个同事共用这个网站，不建议把方舟 API Key 暴露给每个人单独填写。更稳的方式是：

- 服务器统一使用公司方舟 API Key。
- TOS AK/SK 只放服务器环境变量。
- 后续再加登录和内部权限表，控制每个同事能用哪些真人 Asset ID。

当前版本还没有登录鉴权，部署到公网前至少要加一层公司 VPN、Nginx Basic Auth、Cloudflare Access 或火山负载均衡访问控制。
